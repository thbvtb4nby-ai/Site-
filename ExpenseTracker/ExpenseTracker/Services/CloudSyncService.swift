import Foundation

enum CloudSyncService {
    // TODO: wire SwiftData + CloudKit container, resolve conflicts.
    static func enable() {}
    static func disable() {}
    static var isEnabled: Bool { false }
}
